# 个人记账 APP — 使用说明

> 已交付。直接用，无需安装任何东西。

## 一、立即打开（5 秒上手）

**最简单的方式**：双击 `index.html`，浏览器（Chrome / Edge / Safari）直接打开就行。

**正式使用方式**（推荐，可"加到主屏幕"当 APP 用）：
1. 在电脑或手机上起一个静态服务，比如在 `记账APP/` 目录运行：
   ```
   python -m http.server 8765
   ```
   然后浏览器访问 `http://localhost:8765`。
2. **iPhone** Safari 打开 → 分享 → "添加到主屏幕"，图标是蓝色记账格子。
3. **Android** Chrome 打开 → 右上角菜单 → "添加到主屏幕"。
4. **GitHub Pages / Netlify / Vercel 免费层** 也能直接部署，把这 6 个文件全传上去就行。

> ⚠️ 直接 `file://` 打开能用核心功能（解析、日历、分类、备份），但 **Service Worker 离线缓存** 要求 http(s) 协议，所以"加到主屏幕"必须用 http。如果你不想折腾，本地双击 `index.html` 就够用。

## 二、文件清单（一个完整目录就这 6 个文件）

```
记账APP/
├── index.html      ← 主体（HTML + CSS + JS 全内联）
├── manifest.json   ← PWA 配置
├── sw.js           ← 离线缓存
├── icon.svg        ← 应用图标（矢量）
├── icon-192.png    ← PWA 图标（192×192）
└── icon-512.png    ← PWA 图标（512×512）
```

## 三、第一次使用流程

1. 打开 `index.html`。
2. 点底部 **⚙️ 设置** → **选择文件** → 同时选 `支付宝交易明细.csv` 和 `微信支付账单.xlsx`。
3. 顶部会弹出 toast：`成功导入 17 笔，过滤 0 条退款，去重 0 条`。
4. 回到 **📅 日历**，你应该看到 2026 年 8 月的日历，每格里有当天支出金额，最高的一天底色偏红。

## 四、每月要做的事（10 秒）

1. 导出支付宝账单 → 邮箱收到 CSV。
2. 导出微信账单 → 邮箱收到 xlsx（注意微信只能 CSV 但你这次是 xlsx 格式）。
3. 在「设置」里一次性把这两个文件选上导入。
4. 推荐：点「导出 JSON」存一份本地备份（清浏览器数据不会丢）。

## 五、自动分类逻辑

按以下优先级匹配商户名：

| 优先级 | 来源 | 示例 |
|---|---|---|
| 1 | **你改过的自学习规则** | 你把"米隆多"改成"内部转账"，以后所有米隆多自动归"内部转账" |
| 2 | 内置默认规则 | 含"淘宝"→购物；含"滴滴"→交通；含"北京联通"→通讯；含"平价超市"→超市/日用 … |
| 3 | 兜底 | "其他" |

**怎么改分类**：日历 → 点某天 → 在明细里点分类标签色块 → 弹出 11 类选项 → 选一个 → 自动记住。

## 六、这次真实账单导入后的预期结果（已自动化测试通过）

| 来源 | 笔数 | 金额 | 已自动分类情况 |
|---|---|---|---|
| 支付宝 | 1 笔 | ¥30.00 | 上海稀宇科技 → 其他（默认，DeepSeek 积分）|
| 微信 | 16 笔 | ¥449.20 | 详见下表 |
| **合计** | **17 笔** | **¥479.20** | |

**微信 16 笔自动分类结果**：

| 商户 | 分类 | 金额 |
|---|---|---|
| 淘宝平台商户 (3 笔) | 购物 | ¥24.5 + 29.4 + 23.9 = ¥77.80 |
| 平价超市 (3 笔) | 超市/日用 | ¥3 + 4 + 20 = ¥27.00 |
| 北京联通、中国移动 | 通讯 | ¥9 + 40 = ¥49.00 |
| 滴滴出行 | 交通 | ¥22.30 |
| 电子政务服务、出入境证件费、杭州深度求索、米隆多、北京学彬多达五金、万银 | 其他 | ¥303.10 |

> 你大概需要手动改 2~4 笔（杭州深度求索是 DeepSeek API 充值，建议归到"通讯"；米隆多是给自己转账，分类无意义，建议也归"其他"），改完后 8 月再导就不用再动了。

## 七、你可能想问的

**Q：localStorage 容量够吗？**
A：一年几千笔大约 200KB。10 年内不用换 IndexedDB。

**Q：清浏览器数据会丢吗？**
A：会。所以每月导出 JSON 备份（设置页第二个按钮），需要恢复就选回 JSON。

**Q：能多设备同步吗？**
A：**不做**（按方案要求）。同设备浏览器 / 加到主屏幕的 PWA 是同一份 localStorage。

**Q：收入怎么办？**
A：**MVP 不做**。但解析器保留了 `direction` 字段，以后想加，可以在 `parseBill` 里把过滤条件从 `direction !== '支出'` 改成收集所有方向。

**Q：能撤销"删除分类规则"吗？**
A：删了就删了，重新改一笔即重新学习。

## 八、已知边界（不是 bug）

- 微信账单是 **CSV 也可以**（但你这次导的是 xlsx），解析器都支持。
- 支付宝新版本改了表头也兼容：解析器按"交易对方""交易分类""收/支"等关键字模糊匹配，不是按列序号。
- 同一文件二次导入会被去重，不会翻倍。
- 跨渠道同一天同金额相同（比如支付宝和微信各买一次 ¥20 的奶茶）**不去重**（方案里说明的）。
- 没在浏览器里截到图（Chrome 远程调试授权需要你点一下弹窗），但已经用 Node 端到端跑了 8/8 断言通过：解析、过滤、分类、去重、自学习都按设计工作。
